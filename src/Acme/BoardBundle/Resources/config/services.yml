parameters:
#    acme_board.example.class: Acme\BoardBundle\Example

services:
#    acme_board.example:
#        class: %acme_board.example.class%
#        arguments: [@service_id, "plain_value", %parameter%]

services:
    salavert.twig.time_ago:
        class: Salavert\Twig\Extension\TimeAgoExtension
        arguments: [@translator]
        tags:
        - { name: twig.extension }
